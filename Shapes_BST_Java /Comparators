package util;
import java.util.Comparator;
import shapes.Shape;

public class Comparators {
    public static Comparator<Shape> byAreaThenId(){
        return Comparator
            .comparingDouble(Shape::area) .thenComparingInt(Shape::getId);
    }
}
